{
	"private": true,
	"name": "gaio",
	"version": "1.0.0",
	"workspaces": ["apps/*", "packages/*"],
	"scripts": {
		"gapp:start": "bun run --filter gapp start",
		"gapp:ios": "bun run --filter gapp ios",
		"gapp:android": "bun run --filter gapp android",
		"gapp:web": "bun run --filter gapp web",
		"check": "bun run -r check",
		"ui:start": "bun run --filter ui start"
	}
}
